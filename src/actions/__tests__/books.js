/* import TYPE from '../types'

describe('books Reducer', () => {
  describe('bookAdd', () => {
    const user = {
      isbn: '12345678',
      name: 'asdf',
      price: '100',
      quantity: '2'
    }
    const addAction = {
      type: TYPE.BOOK.ADD,
      payload: { ...user }
    }
    it('should be create action to add a book', () => {
      expect(action.bookAdd({ user })).toEqual(addAction)
    })
  })
})*/